# Kubernetes Priority, QoS, Preemption & Eviction

---

# 1. Pod Priority

## 1.1 What It Is

Pod Priority defines **importance for scheduling**.

* Higher value → scheduled first
* Lower value → removed first when needed

Priority is defined using a `PriorityClass`.

---

## 1.2 Priority Flow

```mermaid
%%{init: { 'theme': 'base', 'themeVariables': { 'primaryColor': '#1e1e2e', 'primaryTextColor': '#ffffff', 'nodeTextColor': '#ffffff', 'lineColor': '#89b4fa', 'background': '#11111b' } } }%%
flowchart LR
    linkStyle default interpolate basis

    subgraph Definition_Layer [1. Definition]
        PC["PriorityClass <br/> (e.g., value: 1000000)"]
    end

    subgraph Queue_Layer [2. Scheduling Queue]
        PQ{Priority Sort}
        P1[High Priority Pod]
        P2[Low Priority Pod]
    end

    subgraph Logic_Layer [3. Placement Logic]
        Fit{Node Space Available?}
        Bind[Schedule Pod]
        Evict["Preemption: <br/> Evict Lower-Priority Pods"]
    end

    %% Execution Flow
    PC -->|Applied to Pod| P1
    P1 & P2 --> PQ
    PQ --> Fit
    Fit -- "Yes" --> Bind
    Fit -- "No" --> Evict
    Evict --> Bind

```

---

## 1.3 PriorityClass Example

```yaml
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: high-priority
value: 1000
globalDefault: false
description: "High priority workloads"
```

Pod using it:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: high-pod
spec:
  priorityClassName: high-priority
  containers:
  - name: app
    image: nginx
```

Higher `value` = higher scheduling preference.

---
# 2. QoS (Quality of Service)

* QoS determines **how protected a Pod is under resource pressure**.
* QoS is based only on **CPU and Memory requests and limits**.

---

## 2.1 QoS Classes

| QoS Class  | Condition                            | Protection Level |
| ---------- | ------------------------------------ | ---------------- |
| Guaranteed | requests = limits for all containers | Highest          |
| Burstable  | requests < limits                    | Medium           |
| BestEffort | No requests or limits                | Lowest           |

---

## 2.2 QoS Calculation Logic

```mermaid
%%{init: { 'theme': 'base', 'themeVariables': { 'primaryColor': '#1e1e2e', 'primaryTextColor': '#ffffff', 'nodeTextColor': '#ffffff', 'lineColor': '#89b4fa', 'background': '#11111b' } } }%%
flowchart LR
    linkStyle default interpolate basis

    Start([Pod Created]) --> Check{"Are Requests & <br/> Limits Defined?"}

    Check -- "No" --> BE["[BestEffort]"]
    Check -- "Yes" --> Equal{"Requests == Limits?"}

    Equal -- "Yes" --> GR["[Guaranteed]"]
    Equal -- "No" --> BR["[Burstable]"]

    subgraph Eviction_Order ["Node Memory Pressure"]
        direction TB
        BE --> Kill1["1st: Evicted"]
        BR --> Kill2["2nd: Evicted if over request"]
        GR --> Kill3["3rd: Last Resort"]
    end
```

---
## 2.3 Examples

### Guaranteed

```yaml
resources:
  requests:
    memory: "100Mi"
    cpu: "100m"
  limits:
    memory: "100Mi"
    cpu: "100m"
```

Requests equal limits → Guaranteed

---

### Burstable

```yaml
resources:
  requests:
    memory: "100Mi"
  limits:
    memory: "200Mi"
```

Requests less than limits → Burstable

---

### BestEffort

```yaml
containers:
- name: app
  image: nginx
```

No requests or limits → BestEffort

---
# 3. Preemption

Preemption happens **before scheduling**.

When a high-priority Pod cannot be scheduled due to lack of resources, Kubernetes removes lower-priority Pods to free space.

---

## 3.1 Preemption Flow

```mermaid
%%{init: { 'theme': 'base', 'themeVariables': { 'primaryColor': '#1e1e2e', 'primaryTextColor': '#ffffff', 'nodeTextColor': '#ffffff', 'lineColor': '#89b4fa', 'background': '#11111b' } } }%%
flowchart LR
    linkStyle default interpolate basis

    HP([High Priority Pod]) --> Queue["Scheduling Queue <br/> (Sorted by Priority Value)"]
    Queue --> Scheduler{"Try to Schedule"}

    Scheduler -- "Fits" --> Bind["Scheduled to Node"]
    
    Scheduler -- "Doesn't Fit" --> Preempt["Preemption Logic Triggered"]
    
    subgraph Preemption_Process ["Eviction Loop"]
        Preempt --> Select["Select Victims <br/> (Lowest Priority First)"]
        Select --> Evict["Evict Lower Priority Pods"]
        Evict --> Wait["Wait for Graceful Termination"]
    end

    Wait --> Bind


```

> Preemption is controlled by the **scheduler**.

---
# 4. Eviction

Eviction happens **after a Pod is running**.

Triggered by:

* Memory pressure
* Disk pressure
* Node instability

Handled by **kubelet**, not scheduler.

---

## 4.1 Eviction Order Based on QoS

Eviction priority:

1. BestEffort
2. Burstable
3. Guaranteed

### Eviction flow
```mermaid
%%{init: { 'theme': 'base', 'themeVariables': { 'primaryColor': '#1e1e2e', 'primaryTextColor': '#ffffff', 'nodeTextColor': '#ffffff', 'lineColor': '#89b4fa', 'background': '#11111b' } } }%%
flowchart LR
    linkStyle default interpolate basis

    Pressure["[Node Resource Pressure] <br/> (Disk/Memory/PID)"] --> Kubelet{Kubelet Ranks Pods}

    subgraph Eviction_Priority ["Eviction Ranking (QoS Class)"]
        direction TB
        BE["1. BestEffort <br/> (No requests/limits)"]
        BR["2. Burstable <br/> (Usage > Request)"]
        GR["3. Guaranteed <br/> (Critical Workloads)"]
    end

    Kubelet --> BE
    Kubelet --> BR
    Kubelet --> GR

    BE --> E1["[Immediate Eviction] <br/> First to be killed"]
    BR --> E2["[Conditional Eviction] <br/> Killed if exceeding its 'Request'"]
    GR --> E3["[Last Resort] <br/> Only killed if system stability is at risk"]
```

---

# 5. Preemption vs Eviction

| Aspect    | Preemption                | Eviction             |
| --------- | ------------------------- | -------------------- |
| When      | Before scheduling         | After running        |
| Trigger   | High priority Pod blocked | Node pressure        |
| Component | Scheduler                 | Kubelet              |
| Based On  | Priority                  | QoS + resource usage |

---
# 6. How Everything Connects

```mermaid
%%{init: { 'theme': 'base', 'themeVariables': { 'primaryColor': '#1e1e2e', 'primaryTextColor': '#ffffff', 'nodeTextColor': '#ffffff', 'lineColor': '#89b4fa', 'background': '#11111b' } } }%%
flowchart LR
    linkStyle default interpolate basis

    subgraph Admission_Phase ["The Scheduler (Priority)"]
        direction TB
        PC["PriorityClass <br/> (Integer Value)"] --> SD["Scheduling Priority"]
        SD --> Preempt["Preemption Logic <br/> (Kick out to make room)"]
    end

    subgraph Survival_Phase ["The Kubelet (QoS)"]
        direction TB
        RL["Requests & Limits <br/> (YAML Spec)"] --> EO["QoS Class <br/> (G, B, BE)"]
        EO --> Evict["Eviction Logic <br/> (Kick out to save Node)"]
    end

    %% Convergence
    Preempt --> Cluster[("Stable Cluster State")]
    Evict --> Cluster

    %% Comparison Links
    PC -.->|Influences Admission| Cluster
    RL -.->|Influences Retention| Cluster
```

* Priority decides **who gets scheduled first**.
* Preemption removes lower priority Pods.
* QoS decides **who survives node pressure**.
* Eviction removes Pods under resource pressure.

---

# 7. Operational Understanding

High priority + Guaranteed QoS = most protected workload.

No limits + low priority = first to be removed.

Always define:

* CPU requests
* Memory requests
* CPU limits
* Memory limits
* PriorityClass for critical workloads

This ensures predictable scheduling, stable production behavior, and controlled failure handling.
