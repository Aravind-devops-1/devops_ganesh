# Kubernetes Scheduling Terms Explained

Kubernetes decides **where a Pod runs** using **Node labels, taints, tolerations, and affinity rules**.

---

# 1. Node Selector

### What is Node Selector?

**Node Selector** is the **simplest way** to tell Kubernetes:

> “Run this Pod only on Nodes with this label.”

It works by **matching Pod requirements with Node labels**.

### How it works

* Nodes have labels
* Pods request labels
* If labels match → Pod is scheduled

### Example

Node label:

```bash
# Label the Minikube Node
kubectl label node node1 disk=ssd

# verify
kubectl get nodes --show-labels
```

Pod spec:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: demo-pod
spec:
  nodeSelector:
    disk: ssd
  containers:
  - name: nginx
    image: nginx:1.25
```

### Key Characteristics

* Scheduler checks node labels
* minikube node has disk=ssd
* Pod is scheduled successfully

### If Label Does NOT Exist

If you remove the label:

```bash
# Remove label
kubectl label node minikube disk-
```

Result:

```
Pod stays in Pending state
```

---

## 2. Node Affinity

### What is Node Affinity?

**Node Affinity** is an **advanced version of nodeSelector**.

It allows Pods to express:

* **Required rules** (must match)
* **Preferred rules** (best effort)

### Types of Node Affinity

| Type                                            | Meaning      |
| ----------------------------------------------- | ------------ |
| requiredDuringSchedulingIgnoredDuringExecution  | Must match   |
| preferredDuringSchedulingIgnoredDuringExecution | Try to match |

### Example

```bash
# Label the Minikube Node
kubectl label node node1 disk=ssd

# verify
kubectl get nodes --show-labels
```

### Complete Pod YAML with Node Affinity
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: affinity-demo-pod
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: disk
            operator: In
            values:
            - ssd
  containers:
  - name: nginx
    image: nginx:1.25
    ports:
    - containerPort: 80
```

```bash
# Apply:
kubectl apply -f affinity-pod.yaml
```

### Key Characteristics

* Pod-driven
* Flexible rules
* Supports logical expressions
* Still cannot protect Nodes

---

## 3. Taints

### What is a Taint?

A **Taint** is a **Node-level rule** that says:

> “Do NOT schedule Pods here unless they explicitly allow it.”

Taints **repel Pods**.

### Syntax

```
key=value:effect
```

Example:

```bash
kubectl taint nodes node1 role=infra:NoSchedule
```

### Effects

| Effect           | Behavior           |
| ---------------- | ------------------ |
| NoSchedule       | Block new Pods     |
| PreferNoSchedule | Avoid Pods         |
| NoExecute        | Evict running Pods |

---

# 4. Tolerations

### What is a Toleration?

A **Toleration** is a **Pod-level permission** that says:

> “This Pod is allowed to run on tainted Nodes.”

Tolerations **do not schedule Pods**, they only **allow them**.

### Example

```yaml
tolerations:
- key: role
  operator: Equal
  value: infra
  effect: NoSchedule
```

---

## How These Work Together (Very Important)

### Scheduling Flow (Simplified)

1. Node Selector / Node Affinity filters Nodes
2. Taints reject Pods
3. Tolerations allow Pods onto tainted Nodes
4. Scheduler selects best Node

---

## Comparison Table (Very Clear)

| Feature            | Node Selector | Node Affinity | Taints | Tolerations  |
| ------------------ | ------------- | ------------- | ------ | ------------ |
| Applied on         | Pod           | Pod           | Node   | Pod          |
| Controls placement | Yes           | Yes           | Yes    | No           |
| Rejects Pods       | No            | No            | Yes    | No           |
| Allows Pods        | No            | No            | No     | Yes          |
| Logic support      | Exact match   | Advanced      | Strict | Match taints |
| Protects Node      | No            | No            | Yes    | No           |

---

## When to Use What

| Scenario            | Best Choice   |
| ------------------- | ------------- |
| Simple placement    | Node Selector |
| Complex placement   | Node Affinity |
| Protect Nodes       | Taints        |
| Allow specific Pods | Tolerations   |

---

## Easy Mental Model

* **Node Selector** → “Place Pod here”
* **Node Affinity** → “Prefer / require Pod here”
* **Taint** → “Keep Pods out”
* **Toleration** → “Exception pass”

---
<br>





</br>

# Summary

### 1. Phase One: Scheduler Filters (The "Pull")

In this phase, the Kubernetes Scheduler looks at the Pod's YAML and tries to find a Node that matches what the Pod "wants."

* **Node Selector:** This is the most basic check. If the Pod says `disktype: ssd`, the scheduler immediately ignores any node without that label. If no nodes match, the Pod stays **Pending**.
* **Node Affinity (Hard Rules):** Formally known as `requiredDuringSchedulingIgnoredDuringExecution`. These are non-negotiable rules (e.g., "Must be in availability-zone-1"). If this fails, the process stops.
* **Node Affinity (Soft Rules):** Formally known as `preferredDuringSchedulingIgnoredDuringExecution`. These are "nice-to-haves." If a node doesn't match a soft rule, the Pod **can still proceed**; it just gets a lower priority score.

---

### 2. Phase Two: Node Enforcement (The "Push")

Even if the Scheduler finds a perfect match, the **Node** has the final say. This is the **Taints & Tolerations** logic. Think of a Taint as a "Keep Out" sign on the node.

* **Is the Node Tainted?** If the Node has no taints, the Pod is scheduled immediately (**Success**).
* **Matching Toleration?** If the Node *is* tainted (e.g., `dedicated=high-cost-gpu:NoSchedule`), the Pod can only enter if it has a **Toleration** (a "key") in its YAML that matches that specific taint.
* **The Result:** If the Pod has the key, it is **Scheduled**. If not, it is **Rejected**, even if it passed the Node Selector and Affinity checks.

---

### Summary Table: How to Read the Flow

| Logic Step | Source | Type | Effect if Fail |
| --- | --- | --- | --- |
| **Node Selector** | Pod | Filter | Stays Pending |
| **Hard Affinity** | Pod | Filter | Stays Pending |
| **Soft Affinity** | Pod | Preference | Schedules anyway (lower priority) |
| **Taints** | Node | Restriction | Rejected by the Node |



```mermaid
flowchart TD
    Start([Pod Created]) --> Filter1{Node Selector<br/>Match Labels?}
    
    %% Filter Phase
    Filter1 -- No --> Fail[Pod Stays Pending]
    Filter1 -- Yes --> Filter2{Node Affinity<br/>Hard Rules?}
    
    Filter2 -- Fail --> Fail
    Filter2 -- Pass --> Filter3{Node Affinity<br/>Soft Rules?}
    
    %% Preferences (Does not block scheduling)
    Filter3 -- Best Effort --> Gate{Check Taints}

    %% Taint/Toleration Phase (The Gatekeeper)
    subgraph Node_Gate [Node Enforcement]
        Gate --> T{Is Node Tainted?}
        T -- No --> S([Scheduled on Node])
        T -- Yes --> Tol{Matching<br/>Toleration?}
        Tol -- No --> Fail
        Tol -- Yes --> S
    end

    %% Legend/Categories
    subgraph Scheduler_Logic [Scheduler Filters]
        Filter1
        Filter2
        Filter3
    end

  ```
