## 1. Kubernetes Service

**Definition :**
  
  In Kubernetes, Pods are **Ephemeral**
  
  > They can restart and change IP addresses.

  But `Service Discovery` provides a **stable way for applications to find and communicate with each other** inside the cluster.

**Core Functions :**

* **Service Discovery:** Allows Pods to find each other without needing to track individual IP addresses.
* **Load Balancing:** Distributes incoming requests evenly across all healthy Pods selected by a label selector.
* **Abstraction:** Decouples the frontend (consumers) from the backend (the actual Pods), ensuring zero downtime during Pod restarts or scaling.

---

```mermaid
graph LR
    %% 1. The Request
    User((User Request)) -- "1. Hits Service IP" --> SVC

    subgraph ServiceLayer [Service Definition]
        direction TB
        SVC[Service: frontend-svc]
        SEL{{"Selector Logic: <br/> Is label app == web?"}}
    end

    subgraph ClusterNamespace [Namespace: default]
        
        %% 2. The Matching Candidates
        subgraph ValidPods [Matches Endpoints]
            P1[Pod A <br/> labels: app=web, ver=v1]
            P2[Pod B <br/> labels: app=web, ver=v2]
        end

        %% 3. The Non-Matching Candidates
        subgraph InvalidPods [Ignored No Match]
            P3[Pod C <br/> labels: app=db]
            P4[Pod D <br/> labels: app=web, env=dev] 
        end
    end

    %% Logic Flow
    SVC --> SEL
    
    %% The "Watch Loop"
    SEL == "1. YES Traffic Allowed" ==> P1
    SEL == "2. YES Traffic Allowed" ==> P2
    
    SEL -.->|3. Traffic Blocked| P3
    
    %% Note on Partial Matches (Advanced Concept)
    SEL -.->|"4. NO (If selector is strict)"| P4
```
---
### Upstream and Downstream in Kubernetes Microservices

In a Kubernetes-based microservices architecture, service interaction defines direction:

* **Upstream service**: The service that sends a request.
* **Downstream service**: The service that receives and processes that request.

### With Kubernetes Service Layer Included

```mermaid
graph LR
    %% Upstream: The Source of the request
    subgraph Upstream [External Traffic]
        User((User)) --> Ing[Ingress]
    end

    %% The Core Application
    subgraph MainApp [Main Application Layer]
        Ing --> App[Order Management System]
    end

    %% Downstream: The dependencies and workers
    subgraph Downstream [Support Services]
        App --> Pay[Payment Service]
        App --> Inventory[Inventory Service]
        Pay --> DB[(Payment DB)]
    end
```
---
## 2. Service Types
1.  **ClusterIP** – Internal service-to-service communication
2.  **NodePort** – External access via node IP and static port
3.  **LoadBalancer** – Production-grade external exposure using cloud LB
4.  **ExternalName** – DNS mapping to external services
5.  **Headless** – Direct Pod-level access without load balancing

### Overall Service types
```mermaid
flowchart TD
    S([Kubernetes Service Types])

    subgraph ExternalAccess [External Access]
        L["<b>LoadBalancer</b>"]
        N["<b>NodePort</b>"]
    end

    subgraph InternalAccess [Internal Access]
        C["<b>ClusterIP (Default)</b>"]
        H["<b>Headless</b>"]
    end

    subgraph DNSRedirect [DNS Mapping]
        E["<b>ExternalName</b>"]
    end

    %% Hierarchical Logic
    S --> L
    L --> N
    N --> C
    
    %% Special Cases
    S --> H
    S --> E

```

---

## A. ClusterIP Service (Default – Internal Access)
### ClusterIP Service

**Definition**

A **ClusterIP Service** exposes an application **inside the Kubernetes cluster only** using a **stable internal IP and DNS name**. It is the **default Service type**.

**Functions**

* Provides internal service discovery via DNS
* Load-balances traffic across matching Pods
* Enables secure **east-west (service-to-service)** communication
* Decouples Pod lifecycle from network access

**Limitations**

* Not accessible from outside the cluster
* Requires Ingress, NodePort, or LoadBalancer for external access
* Depends on cluster networking (not reachable from host by default)

```mermaid
graph TD
    subgraph K8sCluster [Kubernetes Cluster]
        
        %% 1. The Consumers
        subgraph Consumers [Frontend Namespace]
            A[Pod A<br/>Client] 
            B[Pod B<br/>Client]
        end

        %% 2. The Abstraction Layer (Service & Endpoints)
        subgraph ServiceLayer [Service Abstraction]
            svc{{ClusterIP Service<br/>IP: 10.96.0.1<br/>Port: 80}}
            ep([Endpoints Object<br/>List: 172.17.0.4:5000, ...])
        end
        
        %% 3. The Actual Destinations
        subgraph Backends [Backend Namespace]
            P1[Backend Pod 1<br/>IP: 172.17.0.4<br/>TargetPort: 5000]
            P2[Backend Pod 2<br/>IP: 172.17.0.5<br/>TargetPort: 5000]
        end

        %% Connections
        A -- "1. curl http://db-service:80" --> svc
        B -- "1. curl http://db-service:80" --> svc

        svc -.->|"2. Looks up IPs"| ep
        ep == "3. Forwards Traffic" ==> P1
        ep == "3. Forwards Traffic" ==> P2
    end
```

---
## B. NodePort Service (External via Node IP)

**Definition**

A **NodePort Service** exposes an application externally by opening a **static port on every cluster node**, allowing access via `<NodeIP>:<NodePort>`.

**Functions**

* Enables external access without a cloud load balancer
* Routes traffic from any node to backend Pods
* Automatically load-balances traffic across Pods
* Useful for testing, demos, and lab environments

**Limitations**

* Limited port range (`30000–32767`)
* Not secure by default (exposes node IPs)
* Not suitable for production at scale
* Requires users to know node IP addresses

```mermaid
graph TD
    User((External User))
    
    subgraph K8sCluster [Kubernetes Cluster Network]
        
        %% 1. The Entry Points (Nodes)
        subgraph NodeA [Worker Node A <br/> IP: 192.168.1.10]
            NP1[NodePort: 30080]
            PodA[Pod A]
        end

        subgraph NodeB [Worker Node B <br/> IP: 192.168.1.11]
            NP2[NodePort: 30080]
            PodB[Pod B]
        end

        %% 2. The Logical Routing Layer (iptables/kube-proxy)
        SVC{{"Service (ClusterIP)"}}
    end

    %% Flow 1: Direct Hit
    User -- "1. Request to Node A" --> NP1
    NP1 --> SVC
    SVC -.->|Traffic stays local| PodA

    %% Flow 2: The "Hop" (Cross-Node)
    User -- "2. Request to Node B" --> NP2
    NP2 --> SVC
    SVC == "Network Hop (SNAT)" ==> PodA
```
---
## C. LoadBalancer Service (Cloud Provider)

**Definition**

A **LoadBalancer Service** exposes an application externally by provisioning a **cloud provider–managed load balancer** that routes internet traffic to Kubernetes Pods.

**Functions**

* Provides a public IP or DNS for external access
* Distributes traffic across backend Pods
* Integrates with cloud provider networking (AWS, GCP, Azure)
* Simplifies production-grade external exposure

**Limitations**

* Requires a supported cloud environment
* Incurs additional cloud cost
* Slower to provision compared to ClusterIP
* Limited control compared to Ingress controllers


```mermaid
graph TD
    %% Actors
    User((Internet User))
    
    subgraph CloudProvider [AWS / GCP / Azure Infrastructure]
        
        %% The External Resource
        CLB{{"Cloud Load Balancer <br/> (e.g., AWS ALB / ELB) <br/> Static IP: 1.2.3.4"}}
        
        %% The Control Plane Logic
        API[Cloud API]
    end

    subgraph K8sCluster [Kubernetes Cluster]
        
        %% Control Plane
        subgraph ControlPlane [Control Plane]
            CCM[Cloud Controller Manager]
        end

        %% Data Plane
        subgraph WorkerNodes [Data Plane]
            direction TB
            
            subgraph Node1 [Worker Node A]
                NP1[NodePort: 32001]
                PodA[Backend Pod A]
            end
            
            subgraph Node2 [Worker Node B]
                NP2[NodePort: 32001]
                PodB[Backend Pod B]
            end
        end
        
        %% Logical Definition
        S[Service: type=LoadBalancer]
    end

    %% TRAFFIC FLOW (The Path of the Packet)
    User == "1. HTTPS Request" ==> CLB
    CLB == "2. Route to NodePort" ==> NP1 & NP2
    NP1 --> PodA
    NP2 --> PodB

    %% PROVISIONING FLOW (The Automation)
    S -.->|"1. Watches Service"| CCM
    CCM -.->|"2. API Call: Create LB"| API
    API -.->|"3. Provisions"| CLB
```
---


## D. ExternalName Service (DNS Alias)

**Definition**

An **ExternalName Service** maps a Kubernetes Service name to an **external DNS name**, acting as a **DNS alias** instead of routing traffic to Pods.

**Functions**

* Provides internal DNS-based access to external services
* Avoids hardcoding external URLs in applications
* Simplifies service abstraction and configuration management
* Useful for accessing external databases or APIs

**Limitations**

* No load balancing or proxying by Kubernetes
* Works only at the DNS level
* Does not create Endpoints or a ClusterIP
* Cannot be used for TCP/UDP traffic control

```mermaid
graph LR
    subgraph K8sCluster [Kubernetes Cluster]
        
        %% The App Layer
        subgraph AppLayer [Namespace: default]
            Pod[Application Pod]
        end

        %% The "Service" (Configuration Only)
        subgraph ConfigLayer [Service Abstraction]
            Svc[("Service: my-db <br/> Type: ExternalName <br/> externalName: db.aws.com")]
        end

        %% The DNS Layer
        subgraph DNSLayer [CoreDNS System]
            CoreDNS{{CoreDNS Server}}
        end
    end

    subgraph AWS [External Cloud Provider]
        RDS[(AWS RDS Database <br/> Endpoint: db.aws.com)]
    end

    %% 1. Configuration (Hidden)
    Svc -.->|"1. Configures CNAME Record"| CoreDNS

    %% 2. The Lookup (Control Path)
    Pod -- "2. DNS Query: 'my-db'" --> CoreDNS
    CoreDNS -- "3. Reply: CNAME 'db.aws.com'" --> Pod

    %% 3. The Connection (Data Path)
    Pod == "4. DIRECT CONNECTION (Bypasses K8s)" ==> RDS

```

---

## E. Headless Service (No ClusterIP)

### Headless Service

**Definition**

A **Headless Service** is a Service with `clusterIP: None` that **does not provide load balancing** and instead returns **direct DNS records for individual Pod IPs**.

**Functions**

* Enables direct Pod-to-Pod communication
* Provides stable network identity for Pods
* Used with StatefulSets for ordered, predictable Pods
* Supports client-side load balancing and service discovery

**Limitations**

* No built-in load balancing
* Clients must handle Pod selection and failover
* Not suitable for stateless, high-traffic services
* Requires DNS-aware applications

```mermaid
graph TD
    subgraph ClientLayer [Client Side]
        Client[Client App]
    end

    subgraph K8sCluster [Kubernetes Cluster]
        
        %% The DNS Component
        subgraph DNSLayer [CoreDNS]
            DNSRecords{{"DNS Lookup: 'my-db'<br/>-- Returns List --<br/>1. 10.244.1.5<br/>2. 10.244.2.8<br/>3. 10.244.3.12"}}
        end

        %% The Service Definition
        Service[("Headless Service<br/>clusterIP: None<br/>selector: app=db")]

        %% The Pods (Stateful Identity)
        subgraph StatefulBackend [StatefulSet: my-db]
            P1[Pod-0 Master <br/>IP: 10.244.1.5]
            P2[Pod-1 Replica <br/>IP: 10.244.2.8]
            P3[Pod-2 Replica <br/>IP: 10.244.3.12]
        end
    end

    %% 1. The Lookup
    Client -- "1. Who is 'my-db'?" --> DNSRecords

    %% 2. The Decision
    DNSRecords -- "2. Here are all 3 IPs" --> Client
    
    %% 3. The Connection (Client Logic)
    Client -.->|"3. I choose Pod-0 Master"| P1
    Client -.->|"3. I choose Pod-1 Read"| P2
    Client -.->|"3. I choose Pod-2 Read"| P3
```
---

## F. Service → Endpoint → Pod Flow (Internal Mechanics)

**Definition**

This flow describes how Kubernetes routes traffic from a **Service** to the actual **Pods** running an application.

**Flow Explanation**

* A **Service** selects Pods using label selectors
* Kubernetes creates an **Endpoints** (or EndpointSlice) object containing Pod IPs and ports
* **kube-proxy** uses these endpoints to route traffic to healthy Pods
* Traffic is load-balanced across available Pods

**Key Points**

* Services never talk to Pods directly
* Endpoints update automatically as Pods scale or restart
* Ensures stable access despite Pod IP changes

```mermaid
graph TD
    %% 1. The Control Plane (The Brain)
    subgraph ControlPlane [Control Plane Loop]
        API[API Server]
        EC[[Endpoint Controller]]
        
        %% Logic: Watching Pods, Updating Endpoints
        EC -- "1. Watches Pod IPs" --> API
        API -- "2. Updates Endpoints Object" --> ETCD[(etcd Store)]
    end

    %% 2. The Node Level (The Muscle)
    subgraph WorkerNode [Worker Node]
        
        %% The Enforcer
        KP[kube-proxy]
        
        %% The Network Rules
        subgraph NetRules [Linux Kernel / iptables]
            VIP{{"Virtual Service IP <br/> (Rules)"}}
        end
        
        %% The Workloads
        Client[Client Pod]
        Target[Target Pod]
    end

    %% Syncing Logic
    API -.->|"3. Notifies changes"| KP
    KP -.->|"4. Writes iptables rules"| VIP

    %% Traffic Flow (Data Plane)
    Client == "5. Sends Traffic" ==> VIP
    VIP == "6. DNAT (Redirection)" ==> Target
```

---

## 3. When to Use Which Service

| Service Type | Use Case               |
| ------------ | ---------------------- |
| ClusterIP    | Internal microservices |
| NodePort     | Testing / labs         |
| LoadBalancer | Public applications    |
| ExternalName | External DB / API      |
| Headless     | Stateful apps          |
